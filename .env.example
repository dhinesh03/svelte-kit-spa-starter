# Azure AD / MSAL Authentication (optional)
# Set these to enable Azure AD auth for routes under (protected)/.
# Leave empty to run the app without authentication.
VITE_AZURE_TENANT_ID=
VITE_AZURE_CLIENT_ID=
VITE_ACCESS_SCOPES=api://your-api/.default
